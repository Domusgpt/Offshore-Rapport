name: Deploy to Fly.io

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy app
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        continue-on-error: true
      
      - name: Lint and validate
        run: |
          npm run lint || echo "Linting issues detected but continuing"
          npm run validate || echo "Validation issues detected but continuing"
        continue-on-error: true
      
      - name: Setup Flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master
      
      - name: Deploy to Fly.io
        run: |
          echo "Deploying to app offshore-rapport"
          flyctl deploy --remote-only --app offshore-rapport --config fly.toml --now
          echo "Deployment completed"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}